package com.securechat.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SecurityBeans {
    // PasswordEncoder bean is now defined in SecurityConfig to avoid duplication
}